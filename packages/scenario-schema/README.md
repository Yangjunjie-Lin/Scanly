# @scanly/scenario-schema

Versioned, runtime-validated capture scenarios for the Scanly SDK v2 preview. Schema `2.0` is the only accepted version in this alpha; future migrations are explicit rather than silently coercing unknown configuration.
