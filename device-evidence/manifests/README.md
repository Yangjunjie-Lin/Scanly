# Session manifests

Place immutable, privacy-reviewed per-session protocol metadata here when physical testing begins. Every file must reference the fixed target IDs from `device-lab/test-targets/ground-truth.json`.
