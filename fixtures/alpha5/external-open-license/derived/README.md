# Derived external validation assets

Store only explicitly derived crops, rotations, redactions, or resizes here. Keep originals in the family directory unchanged and record every operation in the source manifest's `modifications` array. This directory is empty until accepted originals are available.
