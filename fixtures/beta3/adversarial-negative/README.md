# Industrial adversarial negatives

The manifest expands to 120 deterministic non-code surfaces (12 categories × 10 seeds). They resemble common industrial textures but contain no encoded payload. Generation is local and deterministic; the expected result for every surface is `no-confirmed-result`.
