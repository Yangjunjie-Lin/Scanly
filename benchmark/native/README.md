# Native development benchmark

This benchmark records development-only P50/P95 measurements for cold context
initialization and warm small, large, single-code, and multi-code Y-plane
decodes. It deliberately defines no commercial or release threshold. Runner,
compiler, architecture, and commit metadata belong in the CI artifact that
contains the generated JSON report; measurements are not release evidence.
