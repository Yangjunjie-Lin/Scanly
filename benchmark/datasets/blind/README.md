# Blind evaluation boundary

This directory intentionally contains no evaluation images. A future blind dataset must be access-controlled or physically separated from tuning data, versioned independently, and evaluated without exposing labels to pipeline authors before a run is frozen.
