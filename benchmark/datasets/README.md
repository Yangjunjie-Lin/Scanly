# Benchmark datasets

- `fixtures/manifest.json` contains deterministic generated images and project-owned real images.
- Generated negative and adversarial images are retained in the same canonical manifest so false-positive gates are reproducible.
- A blind split must be stored and governed separately; no blind images are present in this branch.

Dataset results are internal regression evidence, not a commercial-competitor or universal-accuracy claim.
